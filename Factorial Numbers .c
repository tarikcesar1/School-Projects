#include<stdio.h>
int main()
{
	int i,fact=1,number;
	printf("enter the number");
	scanf("%d\n",&number);
	{
		for(i=1;i<=number;i++)
	fact=("%d",fact);
  printf("%d",fact);
}
  return 0;
}
