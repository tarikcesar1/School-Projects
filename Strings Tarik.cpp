#include <stdio.h>
#include <string.h>
int main()
{
char string[] = "TARIK";
printf("%s\n", string);
int length = 0;
length = strlen(string);
printf("Length of string str is %d", length);
return 0;
}

